# testkern

This kernel package was generated by `diva new --kernel`.

It builds as a freestanding object through the current Diva toolchain.

## Validate

```sh
diva check .
diva emit-ir .
diva build .
```
