# Manual / scratch programs

Ad-hoc `.diva` snippets historically left at the repo root. They are **not** part of
`tests/run.sh`, `tests/negative.list`, or `tests/strict-pure.list`.

Prefer `examples/` for demos that should stay green on the native suite.
