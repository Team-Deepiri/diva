// Package dep alias (Issue #24): `import "math_lib"` resolves via package.diva
// `dep.math_lib = "..."`, then that package's entry file.
import "math_lib"

extern func print_int(x: int): void

func main(): int {
    print_int(triple(14))
    return 0
}
