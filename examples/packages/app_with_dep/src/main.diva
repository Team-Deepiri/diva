// Native merge resolves string paths on disk (no `pkg/` dep alias yet); keep a relative .diva path.
import "../../math_lib/src/lib.diva"

extern func print_int(x: int): void

func main(): int {
    print_int(triple(14))
    return 0
}
