extern func print_int(x: int): void

// Pinned seed: no generics / traits / classes here (parser + native subset). Output is
// still "7" so integration expectations stay stable; see tests/cases for full trait tests.
func main(): int {
    print_int(3 + 4)
    return 0
}
